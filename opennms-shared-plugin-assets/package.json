{
  "name": "opennms-shared-plugin-assets",
  "private": true,
  "description": "Lab: share UI assets between several OpenNMS UI-extension plugins through one host folder served by Jetty.",
  "workspaces": [
    "packages/shared-assets",
    "plugins/node-inventory/ui",
    "plugins/icon-catalog/ui"
  ],
  "scripts": {
    "build:helper": "npm run build -w @onms-lab/shared-assets",
    "build:ui": "npm run build:helper && npm run build -w node-inventory-ui -w icon-catalog-ui",
    "test": "npm test -w @onms-lab/shared-assets",
    "typecheck": "npm run build:helper && npm run typecheck --workspaces --if-present",
    "check:plugins": "python3 scripts/check_plugins.py"
  },
  "engines": {
    "node": ">=18.18"
  }
}
